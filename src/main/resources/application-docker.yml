spring:
  jpa:
      database-platform: org.hibernate.dialect.MySQL5Dialect
      hibernate:

        ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://pokedex-mysql:3306/test
    username: root
    password: example
    driver-class-name: com.mysql.jdbc.Driver

    #initialize: true